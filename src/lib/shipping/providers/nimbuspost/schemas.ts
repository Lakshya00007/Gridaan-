import { z } from 'zod';

// Intentionally empty until Gridaan obtains the official NimbusPost API
// reference from the seller panel or generated API credentials.
export const documentedNimbusPostStatusMap = {} as const;

export const nimbusPostUnavailableResponseSchema = z.never();
